# API-Keys — kopiere nach .env und fülle aus. .env ist gitignored.
# Nur eintragen, was du wirklich nutzt. Leerlassen = ungenutzt.

# Beispiel (nur falls eigener Outreach/Enrichment-Workflow):
# FIRECRAWL_API_KEY=
# ZEROBOUNCE_API_KEY=
